package com.arkoisystems.arkoicompiler.stage.lexer.token.enums;

public enum OperatorType
{
    
    PLUS_PLUS,
    PLUS,
    MINUS_EQUALS,
    MINUS_MINUS,
    MINUS,
    EQUALS,
    ASTERISK_EQUALS,
    ASTERISK_ASTERISK,
    ASTERISK,
    SLASH_EQUALS,
    SLASH,
    PERCENT_EQUALS,
    PERCENT,
    PLUS_EQUALS
    
}