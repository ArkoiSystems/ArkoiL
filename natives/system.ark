import "variadic"

fun printf(message: u8*, ...): i32

fun vprintf(message: u8*, args: va_list*): i32 

fun println(message: u8*, ...): i32 {
    const args = va_start()
    
    const length = vprintf(message, args)
    printf("\n")
    
    va_end(args)
    return length
}
